<%- include('partials/header', { title }) %>

<main class="content-container">
    <h1><%= title %></h1>
    <p>Explore our service initiatives by category:</p>

    <ul class="categories-list">
        <% categories.forEach(category => { %>
            <li><%= category.name %></li>
        <% }); %>
    </ul>
</main>

<%- include('partials/footer') %>